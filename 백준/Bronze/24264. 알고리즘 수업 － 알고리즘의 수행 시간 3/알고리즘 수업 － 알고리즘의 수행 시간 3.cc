#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main(void)
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	ll n;
	cin >> n; 
	cout << n * n << "\n";
	cout << 2 << "\n";

	return 0;
}