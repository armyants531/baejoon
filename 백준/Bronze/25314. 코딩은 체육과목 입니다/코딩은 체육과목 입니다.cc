#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int N, M;
    cin >> N;
    int q = N / 4;
    for (int i = 0; i < q; i++) {
    	cout << "long ";
	}
	cout << "int" << "\n";
    
    return 0;
}