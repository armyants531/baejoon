#include <bits/stdc++.h>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	vector<vector<char>> arr(5, vector<char>(15, '+'));
	int n;
	cin >> n;
	cout << n << "\n";
	cout << 1 << "\n";
	
	return 0;
}