// 꼬마 정민  // implementation
#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

ll A, B, C;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);
  cin >> A >> B >> C;
  cout << A + B + C << "\n";
  
  return 0;
}